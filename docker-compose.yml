services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: earlyflag_db
      POSTGRES_USER: earlyflag
      POSTGRES_PASSWORD: earlyflag123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: